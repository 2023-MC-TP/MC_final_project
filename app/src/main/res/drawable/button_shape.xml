<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFB700" /> <!-- 버튼의 배경색을 지정합니다. 여기서는 빨간색으로 설정했습니다. -->
    <corners android:radius="0dp" /> <!-- 버튼의 모서리를 둥글게 만듭니다. 원하는 크기로 지정할 수 있습니다. -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" /> <!-- 버튼 내부의 내부 여백을 지정합니다. 원하는 크기로 지정할 수 있습니다. -->
</shape>
